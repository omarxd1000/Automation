Things to know :
	1-For the models to Work you need to have a '.env'
	  file with groq api key in the format : 'GROQ_API_KEY = "KEY" '

	2-For google services and tokens you will need to provide.
	  the credentials file of a google account as a user,
	  as the given code is just for testing or to be a part of
	  more complete project.